# actix-sandbox

```bash
# cargoのインストール
curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh

# WEBアプリケーションの起動
cd hello-world
cargo run

# watch mode
cargo install cargo-watch --locked
cargo watch -x run
```
